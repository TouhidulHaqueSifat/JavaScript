let p = 10, q = 1, r = 5;

if(p>=q && p >= r) console.log("P is the largest number "+p);
else if(q >= p && q >= r) console.log("Q is the largest number "+ q);
else console.log("R is the largest number", r);